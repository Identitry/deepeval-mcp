# ==============================================================================
# LLM API KEYS - At least ONE is required for DeepEval to run evaluations
# ==============================================================================
# OpenAI API key (recommended) - https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic API key (optional) - https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Google API key (optional) - https://console.cloud.google.com/
GOOGLE_API_KEY=your-google-key-here

# ==============================================================================
# AUTHENTICATION
# ==============================================================================
# API Authentication (optional: comma-separated list)
# Protects BOTH /mcp/* and /wrapper/* endpoints with X-API-Key header validation
# Leave blank or unset to disable authentication (NOT recommended for production!)
#
# If not set:
#   - Authentication is DISABLED for both /mcp/* and /wrapper/* endpoints
#   - The wrapper's insecure default "deepeval-default-key" is overridden with empty string
#
# Examples:
#   Single key:    API_KEYS=my-secret-key-123
#   Multiple keys: API_KEYS=key1,key2,key3
#
# Multiple keys allow you to:
#   - Issue different keys to different clients
#   - Rotate keys without downtime (add new, migrate, remove old)
#
API_KEYS=your-api-key-here

# ==============================================================================
# ADVANCED SETTINGS (usually don't need to change these)
# ==============================================================================
# In-process import path for the embedded deepeval wrapper
# Default: app.main (due to PYTHONPATH=/app:/app/deepeval_wrapper)
# Only override if you fork the wrapper's layout
# DEEPEVAL_WRAPPER_IMPORT_PATH=app.main

# ASGI app target for the wrapper
# Default: auto-detected from DEEPEVAL_WRAPPER_IMPORT_PATH
# DEEPEVAL_WRAPPER_ASGI_TARGET=app.main:app

# Timeout for wrapper ASGI calls (in seconds)
# Default: 30
# DEEPEVAL_HTTP_TIMEOUT=30

# ==============================================================================
# LOCAL DEVELOPMENT (only needed when running without Docker)
# ==============================================================================
# PYTHONPATH=./src:./src/deepeval_wrapper
